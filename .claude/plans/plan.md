# Line Duty — План разработки

> Аркада на разведении потоков (тема A «Диспетчер метро»): фигуры едут сверху вниз, игрок рисует им маршруты, ворота своего цвета принимают, касание двух фигур — конец забега.
> Стек: Flutter + Cubit (flutter_bloc) + get_it + go_router + Hive + Flame. Без сети.
> Спека: [my_docs/SPEC.md](../my_docs/SPEC.md) · UI-референс для скиллов: [shared/line_duty_ui_reference.md](../shared/line_duty_ui_reference.md)

---

## Текущий статус: **каркас и играбельное ядро собраны (2026-09-22), нужна проверка на устройстве и тюнинг «на ощупь»**

---

## Что уже сделано (2026-09-22)

### Инфраструктура
- [x] Проект создан по архитектуре WasDrop: core / core_ui / domain / data / features / navigation
- [x] DI на get_it (`appLocator`, `setupAppScope(flavor)`), Hive-боксы `statsBox` / `settingsBox`
- [x] go_router: `/menu` (стартовый), `/game`, `/settings` (fade-переход)
- [x] Flavors dev / prod: Android productFlavors, iOS конфигурации `Debug|Release|Profile-dev|prod` + схемы `dev` / `prod` (скопированы из WasDrop, реклама/Game Center вырезаны), `AppConfig`, плашка «DEV»
- [x] Скрипты `script/` (run, build, prebuild, tests, иконки, звуки), `.run/*.run.xml`
- [x] Иконки iOS/Android + dev с плашкой + нативный экран запуска; 2026-09-22 — настоящая иконка 1024 от дизайнера, подключена adaptive-иконка Android (`mipmap-anydpi-v26`)
- [x] Шрифты Unbounded (заголовок, большой счёт) + Golos Text (UI), переменные, OFL, лицензии в «Лицензиях» (2026-09-22; до этого Space Grotesk без кириллицы)
- [x] Локализация en / ru (easy_localization, `LocaleKeys`)
- [x] Звуки (синтезированные), хаптика, `ButtonFeedback`

### Игра
- [x] Движок на Flame: спавны, ворота, фигуры со значками, рисование маршрута пальцем (стирание за фигурой, пунктирный след), ореол/кольцо пальца, кольцо сближения «!», вспышка столкновения
- [x] Правила: доставка +10, чужие ворота / мимо — конец забега, столкновение — конец забега, продолжение раз за забег (бесплатно), сложность растёт с доведёнными фигурами
- [x] Кубит: счёт, «живой» рекорд, пауза, проигрыш, статистика (рекорд, забеги, доведено)
- [x] Экраны: меню с демо-полем (2b), поле с HUD (1a), пауза, проигрыш (2a), настройки
- [x] Тесты: движок (6) и кубит (5), смоук-тест на симуляторе (`integration_test/game_smoke_test.dart`)

---

## Что нужно сделать

### Фаза 1: Ощущения (ТЕКУЩАЯ)
- [x] **Шрифт с кириллицей (2026-09-22).** Space Grotesk был только латиницей, русский UI шёл системным фолбэком. Выбрана пара Unbounded (заголовок, большой счёт) + Golos Text (UI): `AppFonts.family` / `displayFamily`, лицензии в `LicenseRegistry`, спека § «Шрифт».
- [x] **Стыковка с воротами (2026-09-22).** Палец над *своими* воротами (или выше кромки на `GameTuning.gateDockMargin`) завершает маршрут входом в ворота (X — где палец, но целиком внутри ворот; Y — верхняя кромка), жест закрывается сам, ворота подсвечены (`GateComponent.armed`), линия поверх ворот не рисуется. Чужие ворота не стыкуют: линия идёт как есть, приезд туда — конец забега. Демо в меню тоже стыкуется. Логика жеста — `LineDutyGame.routeStart / routeMove / routeEnd`, тесты в `line_duty_game_test.dart`.
- [ ] **Недорисованный маршрут (предложение, обсуждается 2026-09-22).**
  - Сейчас: после последней точки фигура едет **строго вниз** (как без маршрута), а не по направлению последнего отрезка (сейчас). Правило для игрока: «маршрут — временное отклонение, без него фигура едет вниз». У конца маршрута рисовать короткий пунктирный «хвост» вниз, чтобы было видно, куда она поедет.
  - Приезд в промежуток между воротами — решено 2026-09-22: фигура попадает в ближайшие ворота, которые задевает краем (`gateCatchSlack`); раньше центр в 20-единичном зазоре давал «Не те ворота» даже когда фигура визуально стояла на своих воротах (баг со скриншота).
  - Отклонено: остановка фигуры в конце маршрута (парковка ломает давление спавна), продолжение по последнему отрезку (непредсказуемо, «бесконечная» траектория).
- [ ] **Разрез маршрута (предложение 2026-09-22).** Палец не на фигуре пересекает линию маршрута → маршрут усекается в точке пересечения (настоящее пересечение отрезков, не близость), хвост отваливается с анимацией (0.3–0.4 с: гаснет, сползает вниз), искра в точке среза, звук «щёлк» (в `gen_placeholder_audio.py`), стыковка снимается. Рекомендуемый вариант — **разрез с перенаправлением**: дальнейший путь пальца становится продолжением маршрута от точки среза (стыковка к воротам как обычно); отпустил сразу — маршрут кончается в срезе. Первое пересечение захватывает жест, дальше линии не режутся. Альтернатива — «просто ножницы» (только обрыв, новый маршрут отдельно от фигуры). Зависит от правила «конец маршрута → вниз», делать вместе.
- [ ] Проверить на устройстве: скорость фигур, интервал спавна, радиус захвата пальцем, чувствительность кольца «!» (`GameTuning`)
- [ ] Тряска/подсветка при рисовании: фигура под пальцем чуть увеличивается?
- [ ] Финальное название (спека: TBD; рабочее — Line Duty)

### Фаза 2: Глубина
- [ ] Комбо за серию доставок без перерисовки, множитель
- [ ] Перемешивание порядка ворот между забегами / по ходу забега
- [ ] Новые типы фигур (быстрые, «тяжёлые», с таймером)
- [ ] Ежедневный забег с seed, режим на время
- [x] Онбординг «Как играть» (2026-09-22): 4 шага-карточки поверх первой игры (`TutorialOverlay`/`TutorialArt`), флаг `SettingsModel.tutorialSeen`, повтор из настроек
- [ ] Интерактивный обучающий забег (движок ведёт: одна фигура → «проведи к воротам» → сценарий сближения с «!»); после решения правил «конец маршрута → вниз» и «разрез»

### Темы и бонусы (спека v2, `my_docs/SPEC.md`)
Решения 2026-09-22: Метро остаётся пятой темой и дефолтом; все темы открыты; порядок — темы → бонусы.
- [x] **Этап 1 — система тем + Город (2026-09-22).** `AppPalette`/`AppPalettes` (core_ui), `AppColors` как геттеры над текущей палитрой, `appTheme(palette)` + `systemUiStyle`, remount по ключу палитры в `App`; `SettingsModel.themeId`; скины `FieldSkin` (`metro_skin`, `city_skin`: автобус с поворотом, парковочные ворота, декор-схемы), `DecorLayer`; лента тем `MenuThemeStrip` в меню; обучение рисует скином темы; тесты палитры/скинов, смоук со сменой темы.
- [ ] **Этап 2 — Аквариум, Муравейник, Сад.** По палитре (таблица § 4) и скину на тему: рыбка с хвостом и плавником + грот-арка + лучи/пузырьки/дно (анимация: хвост, пузырьки); муравей (брюшко, голова, 6 лапок 2-кадровым шагом, усики) + холмик с входом + пятна/камешки; божья коровка (панцирь, голова, точки, лапки) + лист с пунктирным кольцом + листья/травинки по краям. Кольцо опасности тёмное в Муравейнике/Саду (уже через `danger`). Анимации требуют `dt` в скине — добавить `time` в `paintUnit`.
- [ ] **Этап 3 — бонусы-пикапы (§ 5).** Шестиугольник 36.7 ед. с пульсирующим кольцом, живёт 8 с, спавн ~20 с не ближе 50 ед. к воротам/спавнам, подбор при проезде фигуры; эффекты: заморозка 3 с (отдельный флаг, рисовать можно), щит (одно столкновение), ×2 на 10 с (кольцо-таймер у счёта), автопилот (`_autoRoute` для одной фигуры); бейдж с названием 1.2 с; таймеры в движке, отображение через `GameListener.onBonusPicked`; переводы `bonus.*`.

### Фаза 3: Релиз
- [ ] Game Center (лидерборд, достижения) — по образцу WasDrop
- [ ] Монетизация: реклама за продолжение (`AppConfig.monetizationEnabled`), премиум без рекламы
- [ ] Листинг App Store (тексты, скриншоты), политика конфиденциальности (`AppConstants`), Apple ID для «Оценить»
- [ ] Переводы de / fr / hu / ja (по образцу WasDrop)

---

## Бэклог / идеи
- Цвета ворот в HUD как легенда для дальтоников
- Режим «ночная смена»: спавны и ворота меняются местами
- Планшет: ограничить ширину поля, как `GameForm.contentMaxWidth` в WasDrop
- Город: покачивание корпуса автобуса на поворотах (спека § 4.1)
- Нативный сплэш `0D131E` — если дефолтом станет светлая тема, будет вспышка при старте

## Архив планов
| Дата | План | Статус |
|---|---|---|
| 2026-09-22 | Каркас проекта + играбельное ядро (этот файл) | сделано |
